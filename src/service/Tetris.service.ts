import * as Rx from 'rxjs';
import { PieceMap } from '../entity/entities';


export class TetrisService{

    constructor(
        private document:Document
    ) {
        
    }

    //随机生成一个方块
    private GenRandomPiece() {
    }
}